# Few-Shot Example: Python Data App

Prompt:

```text
Fix the category totals not matching the sales data.
```

Task:

```text
csv category totals sales data parser aggregation date filter report output
```

Lessons:
- Avoid .venv, __pycache__, dist, output folders.
- Preserve formatting/templates unless requested.
- Validate with tests or representative CLI runs.
